<svg width="1000" height="80" viewBox="0 0 1000 80" xmlns="http://www.w3.org/2000/svg">
<defs><style>
    .pulse { animation: pulse 3s ease-in-out infinite alternate; }
    .pulse2 { animation: pulse 2s ease-in-out infinite alternate; }
    .blink { animation: blink 1s step-end infinite; }
    .scan { animation: scan 6s linear infinite; }
    .float { animation: float 4s ease-in-out infinite; }
    .rotate { animation: rotate 12s linear infinite; }
    .slideIn { animation: slideIn 1.5s ease-out forwards; }
    .glow { animation: glow 2s ease-in-out infinite alternate; }
    .dataScroll { animation: dataScroll 8s linear infinite; }
    .fadeInUp { animation: fadeInUp 1s ease-out forwards; }
    .sweep { animation: sweep 3s ease-in-out infinite; }
    @keyframes pulse { 0%{opacity:.3} 100%{opacity:.9} }
    @keyframes blink { 50%{opacity:0} }
    @keyframes scan { 0%{transform:translateY(-100%)} 100%{transform:translateY(100%)} }
    @keyframes float { 0%,100%{transform:translateY(0)} 50%{transform:translateY(-6px)} }
    @keyframes rotate { 0%{transform:rotate(0deg)} 100%{transform:rotate(360deg)} }
    @keyframes slideIn { 0%{opacity:0;transform:translateX(-20px)} 100%{opacity:1;transform:translateX(0)} }
    @keyframes glow { 0%{filter:drop-shadow(0 0 2px #00e5ff40)} 100%{filter:drop-shadow(0 0 8px #00e5ff80)} }
    @keyframes dataScroll { 0%{transform:translateY(0)} 100%{transform:translateY(-50%)} }
    @keyframes fadeInUp { 0%{opacity:0;transform:translateY(10px)} 100%{opacity:1;transform:translateY(0)} }
    @keyframes sweep { 0%,100%{opacity:.2} 50%{opacity:.6} }
</style></defs>
<rect width="1000" height="80" fill="#0a0a0f"/>

    <polyline points="16,1 1,1 1,16" fill="none" stroke="#00e5ff" stroke-width="1.5" class="glow"/>
    <polyline points="984,1 999,1 999,16" fill="none" stroke="#00e5ff" stroke-width="1.5" class="glow"/>
    <polyline points="1,64 1,79 16,79" fill="none" stroke="#00e5ff" stroke-width="1.5" class="glow"/>
    <polyline points="999,64 999,79 984,79" fill="none" stroke="#00e5ff" stroke-width="1.5" class="glow"/>
<text x="500" y="32" text-anchor="middle" font-family="system-ui,-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,sans-serif" font-size="12" fill="#8b949e" font-style="italic" class="pulse">
    "Any sufficiently advanced technology is indistinguishable from magic." -- Arthur C. Clarke
</text>
<text x="500" y="55" text-anchor="middle" font-family="'Courier New',Consolas,monospace" font-size="9" fill="#1a1f2b" letter-spacing="1">
    DESIGNED WITH PRECISION // BUILT WITH OBSESSION // MAINTAINED FROM ASIA/KOLKATA
</text>
</svg>